# Промпт для продолжения в новой сессии

Скопировать целиком в новую сессию Claude Code, открытую в `/Users/user/Desktop/vibe-proj`.

---

Продолжаем проект Huy Finder. Брейншторм и план уже сделаны, начинаем реализацию.

Контекст:
- Спека: `docs/superpowers/specs/2026-09-19-huy-finder-design.md` — утверждена.
- План: `docs/superpowers/plans/2026-09-19-huy-finder.md` — 17 задач, каждая с тестами, кодом и командами. Ни одна ещё не начата.
- Репозиторий инициализирован, в git только документы. Ключ API в `.env` не класть в git.

Что делать:
1. Прочитай спеку и план.
2. Выполняй план через скилл `superpowers:subagent-driven-development`: свежий субагент на задачу, ревью между задачами, коммит после каждой.
3. Задачи 2–4 и 9, 13 независимы — их можно вести параллельно. Порядок остальных описан в конце плана в разделе «Порядок и зависимости».
4. После Task 17 прогони `npm run typecheck && npm test && npm run build` и покажи мне список ручных проверок из Task 17 Step 5 — их я пройду сам в браузере.

Правила:
- Отвечай по-русски, коротко, без транслита.
- Не расширяй объём: оформление, прогноз трека, история находок — следующая итерация.
- Если тест из плана не проходит на подсаженных буквах генератора — править `STROKES` в `MockSource`, а не ослаблять детекторы.
- Если задача упирается в неоднозначность спеки — остановись и спроси, не додумывай.
